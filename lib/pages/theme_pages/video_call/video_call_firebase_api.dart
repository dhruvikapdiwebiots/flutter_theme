import 'dart:developer';

import '../../../config.dart';

class VideoCallFirebaseApi{


  onJoinChannel(VideoCallController? videoCallController )async{
    dynamic userData  = appCtrl.storage.read(session.user);

  }
}